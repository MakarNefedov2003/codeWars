Получив строку, вы должны вернуть строку, в которой каждый символ (с учетом регистра) повторяется один раз.

function doubleChar(str) {
	var word = '';
  for (var value = 0; value < str.length; value++){
  	word = word + str[value] + str[value];
  };
  return word;
};
